interface:
  display_name: "Run All YAML Reviews"
  short_description: "Review single-test or test-relationship YAMLs"
  default_prompt: "Use $run-all-yaml-reviews to run YAML examples fresh, using single-test review by default or test-relationship review when I request Similar Coverage."
