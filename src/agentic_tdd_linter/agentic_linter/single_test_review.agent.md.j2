{#
Instructions for reviewing a single test.

Inputs:
- file_docstring: Module docstring shared by tests in the source file.
- test: Complete test source exactly as extracted from the source file.
- scenario_type: `happy path` or `failure path` value extracted from `Test Path` in the structured test docstring.
- source_sha256: Invalidates this review packet when the complete source test file changes.
-#}
# Agentic Test Review

## Instructions

Review the shared vocabulary under `## File Docstring` and the structured test docstring and assertions under `## Test Content`.
Update every row in `## Review Scorecard` according to the criteria under `## Review Criteria`.
Set each result to `pass` or `fail`, and provide concrete notes for every failure.

Evaluate each criterion in a fresh subagent. Pass only this Markdown file as the review packet.
Do not inspect repository files, manifests, outer unit tests, prior conversation, or expected assertions.
If context is missing from this file, fail with notes asking the test to include it.

## Review Criteria

### 1x. Requirement Formulation

#### 11. First Sentence Describes Behavior
The first `Requirement Tested` sentence shall state the general behavioral rule: what the system must do.
Fail wording that only describes a use case, mechanics, fixtures, constants, assertions, commands, tags, or test names.

#### 12. Second Sentence Describes a Scenario
{% if scenario_type == "happy path" %}
{% else %}
The second `Requirement Tested` sentence shall describe one concrete failure scenario where the rule applies: when, where, or for whom.
Fail if the scenario sentence is missing or does not describe a concrete failure scenario.
{% endif %}

#### 13. Requirement Is Specific
`Requirement Tested` shall be specific to the behavior and scenario under review.
Fail wording that could describe several tests, appears repeatedly without narrower wording, or can be swapped onto another test with little change.

#### 14. Requirement Is Atomic
`Requirement Tested` shall express one independently verifiable requirement.
Fail wording that combines independently testable behaviors, and recommend splitting them into separate requirements.

### 2x. Verification Formulation

#### 21. Expected Results Appear in Verification Detail
Exact expected results shall appear in `Verification Detail`, not in `Requirement Tested`.

#### 22. Verification Detail Describes Evidence
`Verification Detail` shall state the evidence that proves the requirement rather than only naming mechanics, fixtures, constants, assertions, commands, tags, or test names.

#### 23. Verification Method Matches Exercised Interface
`Verification Method` shall identify the interface whose result the test body directly obtains and asserts.
Fail `verify public function output` when the asserted result comes only from a private helper or private function.

### 3x. Sentence Structure and Logic

#### 31. Sentence Avoids Double Negation
Each sentence shall avoid double negation.
For example, fail `The parser does not reject no input.`; prefer `The parser accepts empty input.`

#### 32. Sentence Uses Subject-Verb-Object Order
Each sentence shall follow Subject -> Verb -> Object.
For example, fail `The parser rejects.` because the sentence omits an object; prefer `The parser rejects empty input.`

#### 33. Subject and Object Are Concise
Each sentence subject and object shall contain no more than two words.
If an idea does not fit, use another sentence with a concrete example.
For example, fail `The configured request parser rejects the malformed user input.`; prefer `The parser rejects text.`

#### 34. Main Verb Is Unambiguous
A sentence's main verb shall not also read as a noun.
For example, fail `The linter checks syntax.` because `checks` can name inspections; prefer `The linter validates syntax.`

#### 35. Conditions Are Explicit
Every condition shall use an explicit condition word such as if, when, unless, or only if.
For example, fail `Empty input triggers rejection.`; prefer `When input is empty, the parser rejects it.`

#### 36. Relative Clauses Name Their Referents
A sentence shall not end with a relative clause that omits referent information.
For example, fail `The parser rejects it, which is invalid.`; prefer `The parser rejects invalid input.`

#### 37. Sentence Expresses One Concept
Each sentence shall communicate one primary idea, requirement, or decision.
For example, fail `The parser trims whitespace and rejects empty input.` because trimming and rejection are separate behaviors; prefer `The parser trims whitespace. The parser rejects empty input.`

#### 38. Behavior Uses Active Voice
`Requirement Tested` shall use active voice when describing behavior.
Fail passive constructions that make the responsible actor implicit or secondary.
For example, fail `Invalid tests are rejected by the linter.`; prefer `The linter rejects invalid tests.`

#### 39. Subject Is Not This, That, These, or Those
A sentence's grammatical subject shall not be `This`, `That`, `These`, or `Those` by itself.
For example, fail `This applies to the repository manifest.`; prefer `The rule applies to the repository manifest.`
Allow these words when they modify a named noun or are not the subject, such as `higher than this test`.

### 4x. Terminology and Reference Clarity

#### 42. Domain Terms Are Defined
Backticks in `Requirement Tested` declare shared local or domain-specific terms.
Every declared term shall exactly match a `Terms:` entry in `File Docstring`.
Backticks elsewhere identify code or literal values, not shared glossary terms.
Do not infer glossary terms from plain, unbackticked wording.
For example, "The parser applies `normalization`." requires a `normalization` glossary entry.

#### 43. Data-Flow Terms Name an Owner
Ambiguous data-flow words such as `input`, `output`, `provided`, and `returns` shall name an explicit owner.
For example, fail `The output contains errors.`; prefer `The parser output contains errors.`

#### 44. Wording Is Concrete
Review wording shall use concrete terms instead of vague or generic language.
For example, fail `The parser handles input correctly.` because `correctly` is vague and does not name the expected behavior; prefer `The parser rejects empty input.`

### 5x. Assertion Quality

#### 51. Assertions Match the Requirement
Every assertion shall either narrowly prove `Requirement Tested` or validate a test input.

#### 52. Input Checks Are Tagged
Every input-validation assertion shall include the exact `# Input check` comment.

#### 53. Assertions Are Self-Contained
The requirement, test inputs, and user-defined expected values shall be defined inside the test body rather than in an external constant, fixture, helper, or shared setup.
## File Docstring

````text
{{ file_docstring }}
````

## Test Content

```
{{ test }}
```

## Review Scorecard

| # | Criterion | Result | Notes |
|---:|---|---|---|
| 11 | First Sentence Describes Behavior | pending | Replace with review evidence. |
| 12 | Second Sentence Describes a Scenario | pending | Replace with review evidence. |
| 13 | Requirement Is Specific | pending | Replace with review evidence. |
| 14 | Requirement Is Atomic | pending | Replace with review evidence. |
| 21 | Expected Results Appear in Verification Detail | pending | Replace with review evidence. |
| 22 | Verification Detail Describes Evidence | pending | Replace with review evidence. |
| 23 | Verification Method Matches Exercised Interface | pending | Replace with review evidence. |
| 31 | Sentence Avoids Double Negation | pending | Replace with review evidence. |
| 32 | Sentence Uses Subject-Verb-Object Order | pending | Replace with review evidence. |
| 33 | Subject and Object Are Concise | pending | Replace with review evidence. |
| 34 | Main Verb Is Unambiguous | pending | Replace with review evidence. |
| 35 | Conditions Are Explicit | pending | Replace with review evidence. |
| 36 | Relative Clauses Name Their Referents | pending | Replace with review evidence. |
| 37 | Sentence Expresses One Concept | pending | Replace with review evidence. |
| 38 | Behavior Uses Active Voice | pending | Replace with review evidence. |
| 42 | Domain Terms Are Defined | pending | Replace with review evidence. |
| 43 | Data-Flow Terms Name an Owner | pending | Replace with review evidence. |
| 44 | Wording Is Concrete | pending | Replace with review evidence. |
| 51 | Assertions Match the Requirement | pending | Replace with review evidence. |
| 52 | Input Checks Are Tagged | pending | Replace with review evidence. |
| 53 | Assertions Are Self-Contained | pending | Replace with review evidence. |

Source SHA256: `{{ source_sha256 }}`
