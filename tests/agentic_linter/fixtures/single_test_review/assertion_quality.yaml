# Test-source examples for agent review.
# Each example contains only `file_docstring`, `test`, and `expected_scorecard`.
# Criteria omitted from `expected_scorecard` are ignored during comparison.
# Criterion comments must match their titles in `single_test_review.agent.md.j2`.
# Each outcome must be `pass` or `fail` followed by an explanation comment.

assertion_quality_001_extra_assertion_proves:
  file_docstring: |
    """Tests in this file validate `calculator` located at `src/examples/calculator.py`.
    `calculator` is responsible for arithmetic calculations.

    For example, this file contains one test function.
    """
  test: |
    def test_adds_numbers() -> None:
        """Test Path: happy path

        Requirement Tested:
        `calculator` returns a positive result after adding positive numbers.

        Verification Method: verify public function output

        Verification Detail:
        The result is positive.
        """

        a = 1
        b = 2
        c = add_2_positive_numbers(a, b)
        assert c > 0
        assert c == a + b
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Positive-number addition belongs to the calculator's arithmetic responsibility.
    14: # Requirement Is Atomic
      pass # The requirement states one positive-result behavior; the unrelated assertion is evaluated by criterion 51.
    51: # Assertions Prove the Stated Requirement
      fail # Exact addition is outside the stated positive-result requirement.

assertion_quality_002_exact_expected_value:
  file_docstring: |
    """Tests in this file validate `calculator` located at `src/examples/calculator.py`.
    `calculator` is responsible for arithmetic calculations.

    For example, this file contains one test function.
    """
  test: |
    def test_adds_numbers() -> None:
        """Test Path: happy path

        Requirement Tested:
        `calculator` adds positive numbers.
        Standard usage: The scenario demonstrates baseline behavior.

        Verification Method: verify public function output

        Verification Detail:
        The result equals `3`.
        """

        first = 1
        second = 2
        expected_result = 3
        result = add_positive_numbers(first, second)
        assert result == expected_result
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Adding positive numbers belongs to the calculator's arithmetic responsibility.
    21: # Verification Detail States Material Outcomes
      pass # The exact result exemplifies the addition contract and belongs in Verification Detail.
    51: # Assertions Prove the Stated Requirement
      pass # The exact observed sum proves the stated addition contract without creating another requirement.
    53: # Test Values Are Local
      pass # The test body defines both inputs and the expected result.

assertion_quality_003_tagged_input_assertions:
  file_docstring: |
    """Tests in this file validate `calculator` located at `src/examples/calculator.py`.
    `calculator` is responsible for arithmetic calculations.

    For example, this file contains one test function.
    """
  test: |
    def test_adds_numbers() -> None:
        """Test Path: happy path

        Requirement Tested:
        `calculator` returns a positive result after adding positive numbers.

        Verification Method: verify public function output

        Verification Detail:
        The result is positive.
        """

        a = 1
        b = 2
        c = add_2_positive_numbers(a, b)
        assert a > 0  # Input check
        assert b > 0  # Input check
        assert c > 0
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Positive-number addition belongs to the calculator's arithmetic responsibility.
    52: # Input Checks Are Tagged
      pass # Both input-validation assertions use the exact `# Input check` comment.

assertion_quality_004_untagged_input_assertions:
  file_docstring: |
    """Tests in this file validate `calculator` located at `src/examples/calculator.py`.
    `calculator` is responsible for arithmetic calculations.

    For example, this file contains one test function.
    """
  test: |
    def test_adds_numbers() -> None:
        """Test Path: happy path

        Requirement Tested:
        `calculator` returns a positive result after adding positive numbers.

        Verification Method: verify public function output

        Verification Detail:
        The result is positive.
        """

        a = 1
        b = 2
        c = add_2_positive_numbers(a, b)
        assert a > 0
        assert b > 0
        assert c > 0
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Positive-number addition belongs to the calculator's arithmetic responsibility.
    52: # Input Checks Are Tagged
      fail # The input-validation assertions omit the exact `# Input check` comment.

assertion_quality_005_per_source_test:
  file_docstring: |
    """Tests in this file validate `repository_validation` located at `src/examples/repository_validation.py`.
    `repository_validation` is responsible for validating repository module coverage.

    Terms:
    - `source module`: A source module is a non-`__init__.py` Python file under src/agentic_tdd_linter. For example, src/agentic_tdd_linter/cli/main.py is a source module.
    - `test module`: A test module is a test_*.py file under tests. For example, tests/cli/test_main.py is a test module.
    """
  test: |
    def test_repository_modules_have_test_files(self) -> None:
        """Test Path: happy path

        Requirement Tested:
        `repository_validation` requires at least one `test module`.
        Standard usage: The scenario demonstrates baseline behavior.

        Verification Method: verify private function output

        Verification Detail:
        `_missing_test_paths` produces `[]`.

        Similar Coverage:
        - Other test: `test_source_module_structure.py::test_rejects_module_without_test_file`
          Relative level: lower than this test
        """

        repo_root = Path(__file__).resolve().parents[2]
        package_root = repo_root / "src" / "agentic_tdd_linter"
        test_root = repo_root / "tests"
        self.assertEqual(
            [],
            _missing_test_paths(
                package_root=package_root,
                test_root=test_root,
            ),
        )
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Source-to-test module coverage belongs to repository validation.
    12: # Second Line Describes a Scenario
      pass # The standard-usage sentence uses the exact baseline-behavior classification.
    13: # Requirement Has the Appropriate Specificity
      fail # Requiring at least one test module omits the per-source correspondence enforced across the repository.
    51: # Assertions Prove the Stated Requirement
      fail # The assertion enforces one matching test path for every source module, which is stronger than requiring at least one test module.
