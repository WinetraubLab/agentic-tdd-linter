# Test-source examples for agent review.
# Each example contains only `file_docstring`, `test`, and `expected_scorecard`.
# Criteria omitted from `expected_scorecard` are ignored during comparison.
# Criterion comments must match their titles in `single_test_review.agent.md.j2`.
# Each outcome must be `pass` or `fail` followed by an explanation comment.

requirement_formulation_001_availability_without_behavior:
  file_docstring: |
    """Tests in this file validate `availability_policy` located at `src/examples/availability_policy.py`.
    `availability_policy` is responsible for selecting availability windows for organizations.

    For example, this file contains one test function.
    """
  test: |
    def test_organization_availability() -> None:
        """Test Path: happy path

        Requirement Tested:
        When organization is home, availability includes 09:00–12:00; when organization is Y, availability excludes 13:00–16:00.
        Specialized usage: For home availability, organization becomes home (instead of Y).

        Verification Method: verify public function output

        Verification Detail:
        Availability contains the expected time ranges.
        """

        availability = compute_availability(organization="home")

        assert "09:00–12:00" in availability
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      fail # The requirement begins with a scenario condition instead of the declared `availability_policy` module.
    11: # First Line States a General Behavioral Rule
      fail # The requirement lists availability examples without stating a general behavior.
    12: # Second Line Describes a Scenario
      pass # The specialized-usage sentence names the use case, input, standard value, and modified value.
    13: # Requirement Has the Appropriate Specificity
      fail # The first line includes particular organization and time-range examples instead of the material availability policy that governs them.
    14: # Requirement Is Atomic
      fail # The requirement combines independent availability behaviors for two organizations.
    15: # Requirement Begins with the Behavior Owner
      fail # The first requirement sentence begins with the `When` scenario condition instead of the behavior owner.
    21: # Verification Detail States Material Outcomes
      fail # Verification Detail says only expected time ranges instead of naming the observed 09:00–12:00 inclusion.
    51: # Assertions Prove the Stated Requirement
      fail # The assertion checks only the home inclusion and does not prove the Y exclusion.

requirement_formulation_002_assertions_without_behavior:
  file_docstring: |
    """Tests in this file validate `input_assertions` located at `src/examples/input_assertions.py`.
    `input_assertions` is responsible for validating test inputs.

    For example, this file contains one test function.
    """
  test: |
    def test_adds_numbers() -> None:
        """Test Path: happy path

        Requirement Tested:
        `input_assertions` pass when `assert a > 0` and `assert b > 0` include `# Input check` tags.

        Verification Method: verify public function output

        Verification Detail:
        The result is positive.
        """

        assert 1 + 1 > 0
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Input-assertion tags belong to input validation.
    11: # First Line States a General Behavioral Rule
      fail # The requirement describes assertion tags rather than system behavior.
    51: # Assertions Prove the Stated Requirement
      fail # The arithmetic assertion does not prove the assertion-tag wording.

requirement_formulation_003_swappable_requirement_case:
  file_docstring: |
    """Tests in this file validate `sentence_validator` located at `src/examples/sentence_validator.py`.
    `sentence_validator` is responsible for validating sentence structure.

    For example, this file contains one test function.
    """
  test: |
    def test_bad_sentence_structure() -> None:
        """Test Path: failure path

        Requirement Tested:
        `sentence_validator` rejects bad sentence structure.

        Verification Method: verify public function output

        Verification Detail:
        The assertion checks a sentence without a verb.
        """

        assert validate_sentence("Apple catapiller") == "fail"
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Sentence rejection belongs to sentence validation.
    13: # Requirement Has the Appropriate Specificity
      fail # Each requirement is generic enough to be swapped onto another sentence-validation test.

requirement_formulation_004_ambiguous_input_case:
  file_docstring: |
    """Tests in this file validate `value_normalizer` located at `src/examples/value_normalizer.py`.
    `value_normalizer` is responsible for preparing values for validation.

    For example, this file contains one test function.
    """
  test: |
    def test_adds_numbers() -> None:
        """Test Path: happy path

        Requirement Tested:
        The input is normalized before validation.

        Verification Method: verify public function output

        Verification Detail:
        The result is positive.
        """

        assert 1 + 1 > 0
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      fail # The requirement begins with ambiguous input instead of the declared `value_normalizer` module.
    43: # Data-Flow Terms Name an Owner
      fail # Input does not name the component or caller that owns it.

requirement_formulation_005_ambiguous_output_case:
  file_docstring: |
    """Tests in this file validate `value_normalizer` located at `src/examples/value_normalizer.py`.
    `value_normalizer` is responsible for producing normalized values.

    For example, this file contains one test function.
    """
  test: |
    def test_adds_numbers() -> None:
        """Test Path: happy path

        Requirement Tested:
        The output includes the normalized value.

        Verification Method: verify public function output

        Verification Detail:
        The result is positive.
        """

        assert 1 + 1 > 0
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      fail # The requirement begins with ambiguous output instead of the declared `value_normalizer` module.
    43: # Data-Flow Terms Name an Owner
      fail # Output does not name the component or operation that produces it.

requirement_formulation_006_ambiguous_returns_case:
  file_docstring: |
    """Tests in this file validate `value_provider` located at `src/examples/value_provider.py`.
    `value_provider` is responsible for supplying computed values.

    For example, this file contains one test function.
    """
  test: |
    def test_adds_numbers() -> None:
        """Test Path: happy path

        Requirement Tested:
        Returns the expected value.

        Verification Method: verify public function output

        Verification Detail:
        The result is positive.
        """

        assert 1 + 1 > 0
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      fail # The requirement omits the declared `value_provider` module and has no grammatical subject.
    32: # Sentence Uses Subject-Verb-Object Order
      fail # The sentence omits the subject that performs the return operation.
    43: # Data-Flow Terms Name an Owner
      fail # Returns does not name the component or operation that owns the returned value.

requirement_formulation_007_ambiguous_provided_case:
  file_docstring: |
    """Tests in this file validate `artifact_table` located at `src/examples/artifact_table.py`.
    `artifact_table` is responsible for constructing artifact rows from templates.

    For example, this file contains one test function.
    """
  test: |
    def test_adds_numbers() -> None:
        """Test Path: happy path

        Requirement Tested:
        `artifact_table` adds an artifact row from a template using the provided activity name.

        Verification Method: verify public function output

        Verification Detail:
        The result is positive.
        """

        assert 1 + 1 > 0
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Constructing artifact rows belongs to the artifact table.
    43: # Data-Flow Terms Name an Owner
      fail # Provided does not identify who supplies the activity name.

requirement_formulation_008_missing_file_without:
  file_docstring: |
    """Tests in this file validate `jinja_render_validation` located at `src/examples/jinja_render_validation.py`.
    `jinja_render_validation` is responsible for validating manifest-backed template rendering.

    For example, this file contains one test function.
    """
  test: |
    def test_reports_manifest_missing_source() -> None:
        """Test Path: failure path

        Requirement Tested:
        `jinja_render_validation` reports files named in the render manifest YAML but missing on disk.

        Verification Method: verify public function output

        Verification Detail:
        Validation error names missing source files.
        """

        assert 1 + 1 > 0
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Missing render-manifest files belong to Jinja render validation.
    12: # Second Line Describes a Scenario
      fail # Requirement Tested contains no second sentence describing a concrete missing-file scenario.

requirement_formulation_009_unused_context_without:
  file_docstring: |
    """Tests in this file validate `jinja_manifest_rendering` located at `src/examples/jinja_manifest_rendering.py`.
    `jinja_manifest_rendering` is responsible for rendering templates from manifest context.

    For example, this file contains one test function.
    """
  test: |
    def test_rejects_unused_context_variable() -> None:
        """Test Path: failure path

        Requirement Tested:
        `jinja_manifest_rendering` rejects context variables unused by the source template.

        Verification Method: verify public function output

        Verification Detail:
        Error report names unused context variable.
        """

        source_template = "Hello {{ name }}"
        manifest_context = {"name": "Ada", "unused_title": "Dr"}
        errors = validate_manifest_render(source_template, manifest_context)

        assert "unused_title" in errors
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Unused manifest context belongs to Jinja manifest rendering.
    12: # Second Line Describes a Scenario
      fail # Requirement Tested contains no second sentence describing a concrete unused-context scenario.

requirement_formulation_010_version_equality_described:
  file_docstring: |
    """Tests in this file validate `package_metadata` located at `src/examples/package_metadata.py`.
    `package_metadata` is responsible for exposing package version information.

    Terms:
    - `__version__`: This package attribute exposes the installed linter version. For example, callers read it at runtime.
    """
  test: |
    def test_matches_package_metadata(self) -> None:
        """Test Path: happy path

        Requirement Tested:
        `package_metadata` identifies `__version__`.
        Standard usage: The scenario demonstrates baseline behavior.

        Verification Method: verify private function output

        Verification Detail:
        `_project_version` produces `__version__`.
        """

        repo_root = Path(__file__).resolve().parents[1]
        project_version = _project_version(repo_root / "pyproject.toml")

        self.assertEqual(project_version, __version__)
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Runtime and project versions belong to package metadata.
    13: # Requirement Has the Appropriate Specificity
      fail # Identifies omits the material equality invariant between the project metadata version and runtime version.
    51: # Assertions Prove the Stated Requirement
      fail # The assertion compares two version values, while the requirement claims only that metadata identifies one attribute.
           # Expected requirement: The package version exposed at runtime equals the project metadata version read by `_project_version`.

requirement_formulation_011_version_relationship_is:
  file_docstring: |
    """Tests in this file validate `package_metadata` located at `src/examples/package_metadata.py`.
    `package_metadata` is responsible for exposing package version information.

    Terms:
    - `project.version`: Project.version is the package version field in pyproject.toml. For example, package builds use this value as their distribution version.
    - `__version__`: This package attribute exposes the installed linter version. For example, callers can read it at runtime.
    """
  test: |
    def test_matches_package_metadata(self) -> None:
        """Test Path: happy path

        Requirement Tested:
        `package_metadata` defines `project.version` equal to `__version__`.
        Standard usage: The scenario demonstrates baseline behavior.

        Verification Method: verify private function output

        Verification Detail:
        `_project_version` reads `project.version`.
        The returned value equals `__version__`.
        """

        repo_root = Path(__file__).resolve().parents[1]
        project_version = _project_version(repo_root / "pyproject.toml")

        self.assertEqual(project_version, __version__)
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Equality between project and runtime versions belongs to package metadata.
    11: # First Line States a General Behavioral Rule
      pass # The equality between the project metadata version and runtime version is the architectural contract proved by the test.
    21: # Verification Detail States Material Outcomes
      pass # The equality relationship defines the contract, while Verification Detail states the concrete observations that prove it.

requirement_formulation_012_api_limit_and:
  file_docstring: |
    """Tests in this file validate `source_module_policy` located at `src/examples/source_module_policy.py`.
    `source_module_policy` is responsible for defining public interfaces for source modules.

    Terms:
    - `source module`: A source module is a non-`__init__.py` Python file under src/agentic_tdd_linter. For example, cli/main.py is a source module.
    - `data-only module`: A data-only module exposes declarative values instead of callable behavior. For example, version.py exposes version metadata.
    """
  test: |
    def test_limits_module_public_functions(self) -> None:
        """Test Path: happy path

        Requirement Tested:
        `source_module_policy` restricts every `source module` to at most two public functions unless it is a `data-only module`.
        Standard usage: The scenario demonstrates baseline behavior.

        Verification Method: verify private function output

        Verification Detail:
        `_public_api_violations` produces `[]`.
        Every checked `source module` contains at most two public functions unless it is a `data-only module`.
        """

        repo_root = Path(__file__).resolve().parents[2]
        package_root = repo_root / "src" / "agentic_tdd_linter"
        data_only_modules = {
            Path("indexing_test_functions/extracted_test_record.py"),
            Path("version.py"),
        }
        self.assertEqual(
            [],
            _public_api_violations(package_root, data_only_modules),
        )
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Public-interface limits belong to source-module policy.
    11: # First Line States a General Behavioral Rule
      pass # The API limit and data-only exception are material parts of the architectural policy proved by the test.
    21: # Verification Detail States Material Outcomes
      pass # The limit and exception define the contract, while Verification Detail states the observed absence of violations.

requirement_formulation_013_module_public_function:
  file_docstring: |
    """Tests in this file validate `test_source_module_structure` located at `tests/repository_health/test_source_module_structure.py`.
    `test_source_module_structure` is responsible for enforcing repository source-module and test-module structure.

    Terms:
    - `source module`: A source module is a non-`__init__.py` Python file under src/agentic_tdd_linter. For example, tests/cli/test_main.py matches src/agentic_tdd_linter/cli/main.py.
    - `test module`: A test module is a test_*.py file under tests. For example, tests/cli/test_main.py is a test module.
    - `test-harness module`: A test-harness module is a same-basename non-test Python file beside a test module or inside its folder's test_harness package. For example, test_harness/mock_keyword_identification.py supports test_mock_keyword_identification.py.
    - `data-only module`: A data-only module is indexing_test_functions/extracted_test_record.py or version.py. For example, version.py exposes version data instead of public functions.
    """
  test: |
    def test_limits_module_public_functions(self) -> None:
        """Test Path: happy path

        Requirement Tested:
        `test_source_module_structure` requires every `source module` to expose one or two public functions unless it is a `data-only module`.
        Specialized usage: For data-only exemptions, two declared `data-only module` paths replace the baseline empty exemption set.

        Verification Method: verify private function output

        Verification Detail:
        `_public_api_violations` produces `[]` for the package root when the two `data-only module` paths are exempt.
        `_public_api_violations` identifies controlled modules with zero or three public functions.
        `_public_api_violations` accepts a controlled module with two public functions.
        `_public_api_violations` accepts a controlled three-function `data-only module`.
        """

        repo_root = Path(__file__).resolve().parents[2]
        package_root = repo_root / "src" / "agentic_tdd_linter"
        data_only_modules = {
            Path("indexing_test_functions/extracted_test_record.py"),
            Path("version.py"),
        }
        repository_violations = _public_api_violations(
            package_root,
            data_only_modules,
        )

        with tempfile.TemporaryDirectory() as directory:
            controlled_root = Path(directory)
            (controlled_root / "empty.py").write_text("", encoding="utf-8")
            (controlled_root / "valid.py").write_text(
                "def first(): pass\ndef second(): pass\n",
                encoding="utf-8",
            )
            three_functions = (
                "def first(): pass\n"
                "def second(): pass\n"
                "def third(): pass\n"
            )
            (controlled_root / "excess.py").write_text(
                three_functions,
                encoding="utf-8",
            )
            (controlled_root / "data.py").write_text(
                three_functions,
                encoding="utf-8",
            )

            controlled_violations = _public_api_violations(
                controlled_root,
                {Path("data.py")},
            )

        self.assertEqual([], repository_violations)
        self.assertEqual(
            [
                "empty.py: expected 1-2 public functions, found []",
                "excess.py: expected 1-2 public functions, "
                "found ['first', 'second', 'third']",
            ],
            controlled_violations,
        )
  expected_scorecard:
    14: # Requirement Is Atomic
      fail # The test combines the ordinary public-function limit with the independent data-only-module exemption; these behaviors should be tested separately.

requirement_formulation_014_workflow_baseline_omits:
  file_docstring: |
    """Tests in this file validate `unit_test_workflow` located at `src/examples/unit_test_workflow.py`.
    `unit_test_workflow` is responsible for running repository unit tests.

    Terms:
    - `workflow`: The workflow is `.github/workflows/unit-tests.yml`. For example, repository events can trigger this file.
    - `repository events`: Repository events are pushes and pull requests on any branch. For example, either event triggers the workflow.
    """
  test: |
    def test_unit_workflow_runs_tests(self) -> None:
            """Test Path: happy path

        Requirement Tested:
        `unit_test_workflow` runs tests.
            Standard usage: The scenario demonstrates baseline behavior.

            Verification Method: verify private function output

            Verification Detail:
            `_repository_test_workflow` output contains `python -m unittest discover -s tests`.
            """

            repo_root = Path(__file__).resolve().parents[2]
            workflow = _repository_test_workflow(repo_root)
            self.assertIn("python -m unittest discover -s tests", workflow)
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Executing Python tests belongs to the unit-test workflow.
    11: # First Line States a General Behavioral Rule
      pass # The requirement states that the workflow runs tests.
    13: # Requirement Has the Appropriate Specificity
      fail # Running tests omits the complete Python-suite scope established by unittest discovery under the tests directory.

requirement_formulation_015_record_field_set:
  file_docstring: |
    """Tests in this file validate `extracted_test_record` located at `src/examples/extracted_test_record.py`.
    `extracted_test_record` is responsible for storing language-neutral test data.

    Terms:
    - `ExtractedTestRecord`: ExtractedTestRecord is the shared data structure for one indexed test. For example, it stores a test's path, name, line, syntax node, documentation, and source without a language discriminator.
    """
  test: |
    def test_stores_all_fields_without_language(self) -> None:
            """Test Path: happy path

        Requirement Tested:
        `extracted_test_record` stores path, name, line, syntax node, test docstring, source text, and file docstring fields without a language field.
            Standard usage: The scenario demonstrates baseline behavior.

            Verification Method: verify public function output

            Verification Detail:
            The record exposes `tests/test_sample.py` as its path.
            The record exposes `test_sample` as its name.
            The record exposes `1` as its line.
            The record exposes `None` as its node.
            The record exposes `Sample documentation.` as its docstring.
            The record exposes `def test_sample(): pass` as its source.
            The record exposes `None` as its file docstring.
            The record omits `language`.
            """

            record = ExtractedTestRecord(
                path=Path("tests/test_sample.py"),
                name="test_sample",
                line=1,
                node=None,
                docstring="Sample documentation.",
                source="def test_sample(): pass",
            )

            self.assertEqual(Path("tests/test_sample.py"), record.path)
            self.assertEqual("test_sample", record.name)
            self.assertEqual(1, record.line)
            self.assertIsNone(record.node)
            self.assertEqual("Sample documentation.", record.docstring)
            self.assertEqual("def test_sample(): pass", record.source)
            self.assertIsNone(record.file_docstring)
            self.assertFalse(hasattr(record, "language"))
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # The extracted-test field schema belongs to the extracted-test record.
    14: # Requirement Is Atomic
      pass # The listed fields collectively define one complete record-schema contract.
    51: # Assertions Prove the Stated Requirement
      pass # Each field assertion proves one property named by the record-schema requirement.

requirement_formulation_016_module_identity_is:
  file_docstring: |
    """Tests in this file validate `test_file_docstring_check` located at `src/examples/test_file_docstring_check.py`.
    `test_file_docstring_check` is responsible for validating test-file documentation.

    Terms:
    - `check`: The check validates module-docstring placement in one test file. For example, it accepts a file whose first statement is a string literal.
    """
  test: |
    def test_missing_file_docstring_identifies_module(self) -> None:
        """Test Path: failure path

        Requirement Tested:
        When files omit docstrings, the `check` identifies module scope.
        Specialized usage: For module-scope validation, docstring input becomes absent (instead of leading).

        Verification Method: verify public function output

        Verification Detail:
        Issue name is `<module>`.

        Similar Coverage:
        - Higher Level Test: `test_run_lint_pipeline.py::test_reports_missing_file_docstring`
          Justification: Deeper coverage — This test verifies `<module>`. Pipeline output omits identity.
        """

        with tempfile.TemporaryDirectory() as directory:
            root = Path(directory)
            test_file = root / "tests" / "test_sample.py"
            test_file.parent.mkdir()
            test_file.write_text(
                "def test_sample():\n    assert True\n",
                encoding="utf-8",
            )

            issues = check_test_file_docstring(test_file, root)

        self.assertEqual(["<module>"], [issue.test_name for issue in issues])
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      fail # The requirement begins with a scenario condition instead of the declared `test_file_docstring_check` module.
    15: # Requirement Begins with the Behavior Owner
      fail # The first sentence begins with a When scenario and makes the check, rather than the issue, own the behavior.
    51: # Assertions Prove the Stated Requirement
      pass # The exact `<module>` issue name is concrete evidence that the check identifies module scope.

requirement_formulation_017_repository_policy_described:
  file_docstring: |
    """Tests in this file validate `review_manifest_policy` located at `src/examples/review_manifest_policy.py`.
    `review_manifest_policy` is responsible for preserving review proof in source control.

    Terms:
    - `review manifest`: A review manifest is the tracked proof file for repository test reviews. For example, `tests/agentic_review_manifest.jsonl` is a review manifest.
    - `tracked-file query`: A tracked-file query runs `git ls-files --error-unmatch` for one repository path. For example, the query locates `tests/agentic_review_manifest.jsonl` in the Git index.
    """
  test: |
    def test_review_manifest_is_tracked(self) -> None:
        """Test Path: happy path

        Requirement Tested:
        `tracked-file query` locates `review manifest`.
        Standard usage: The scenario demonstrates baseline behavior.

        Verification Method: verify private function output

        Verification Detail:
        `_tracked_result` produces return code `0`.
        """

        repo_root = Path(__file__).resolve().parents[2]
        result = _tracked_result(
            Path("tests") / "agentic_review_manifest.jsonl",
            repo_root=repo_root,
        )

        self.assertEqual(0, result.returncode, result.stderr or result.stdout)
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      fail # The requirement begins with `tracked-file query` instead of the declared `review_manifest_policy` module.
    15: # Requirement Begins with the Behavior Owner
      fail # The requirement makes the Git query responsible for behavior instead of stating the repository policy that the manifest remains tracked.
    13: # Requirement Has the Appropriate Specificity
      fail # The tracked-file query is an evidence mechanism rather than the material requirement that the manifest remains tracked in source control.

requirement_formulation_018_source_test_pairing:
  file_docstring: |
    """Tests in this file validate `repository_validation` located at `src/examples/repository_validation.py`.
    `repository_validation` is responsible for validating repository module coverage.

    Terms:
    - `source module`: A source module is a non-`__init__.py` Python file under src/agentic_tdd_linter. For example, tests/cli/test_main.py matches src/agentic_tdd_linter/cli/main.py.
    - `test module`: A test module is a test_*.py file under tests. For example, tests/cli/test_main.py is a test module.
    """
  test: |
    def test_rejects_module_without_test_file(self) -> None:
        """Test Path: failure path

        Requirement Tested:
        `repository_validation` checks missing `test module`s.
        Specialized usage: For missing-test rejection, `test module` becomes absent (instead of present).

        Verification Method: verify private function output

        Verification Detail:
        Validation propagates `AssertionError`.

        Similar Coverage:
        - Higher Level Test: `test_source_module_structure.py::test_repository_modules_have_test_files`
          Justification: Diagnostic completeness — This test verifies missing-test assertion. Higher test verifies repository scan.
        """

        with tempfile.TemporaryDirectory() as directory:
            root = Path(directory)
            package_root = root / "src" / "agentic_tdd_linter"
            test_root = root / "tests"
            source = package_root / "agentic_linter" / "orphan.py"
            source.parent.mkdir(parents=True)
            source.write_text("VALUE = 1\n", encoding="utf-8")
            test_root.mkdir()

            with self.assertRaises(AssertionError):
                _assert_modules_have_matching_test_files(
                    package_root=package_root,
                    test_root=test_root,
                )
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Missing source-to-test matches belong to repository validation.
    12: # Second Line Describes a Scenario
      fail # The sentence presents an existing missing-match condition as a specialized input transition instead of directly stating the failure scenario.
    13: # Requirement Has the Appropriate Specificity
      fail # Checks missing test modules omits the material source-to-test correspondence and orphan-source rejection policy.
    17: # Scenario Has the Appropriate Specificity
      fail # Test module absence does not identify the source module whose missing matching test module defines the exercised orphan-source scenario.

requirement_formulation_019_source_test_pairing:
  file_docstring: |
    """Tests in this file validate `repository_validation` located at `src/examples/repository_validation.py`.
    `repository_validation` is responsible for validating repository module coverage.

    Terms:
    - `source module`: A source module is a non-`__init__.py` Python file under src/agentic_tdd_linter. For example, tests/cli/test_main.py matches src/agentic_tdd_linter/cli/main.py.
    - `test module`: A test module is a test_*.py file under tests. For example, tests/cli/test_main.py is a test module.
    """
  test: |
    def test_rejects_module_without_test_file(self) -> None:
        """Test Path: failure path

        Requirement Tested:
        `repository_validation` requires every `source module` to have a matching `test module`.
        Repository validation rejects a `source module` when it lacks a matching `test module`.

        Verification Method: verify private function output

        Verification Detail:
        Validation propagates `AssertionError`.

        Similar Coverage:
        - Higher Level Test: `test_source_module_structure.py::test_repository_modules_have_test_files`
          Justification: Diagnostic completeness — This test verifies missing-test assertion. Higher test verifies repository scan.
        """

        with tempfile.TemporaryDirectory() as directory:
            root = Path(directory)
            package_root = root / "src" / "agentic_tdd_linter"
            test_root = root / "tests"
            source = package_root / "agentic_linter" / "orphan.py"
            source.parent.mkdir(parents=True)
            source.write_text("VALUE = 1\n", encoding="utf-8")
            test_root.mkdir()

            with self.assertRaises(AssertionError):
                _assert_modules_have_matching_test_files(
                    package_root=package_root,
                    test_root=test_root,
                )
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Source-to-test pairing belongs to repository validation.
    12: # Second Line Describes a Scenario
      pass # The sentence directly names the missing-match condition and the validation behavior it triggers.
    13: # Requirement Has the Appropriate Specificity
      pass # The first line states the material per-source pairing policy without test-case or evidence details.
    17: # Scenario Has the Appropriate Specificity
      pass # The second line identifies the orphan-source condition and the rejection behavior without incidental test-case details.

requirement_formulation_020_ordered_review_stages:
  file_docstring: |
    """Tests in this file validate `review_documentation` located at `src/examples/review_documentation.py`.
    `review_documentation` is responsible for describing the repository review workflow.

    Terms:
    - `reviewer identity`: A reviewer identity records the agent and model that completed a review. For example, `codex:gpt-5.5` is a reviewer identity.
    """
  test: |
    def test_readme_shows_review_workflow(self) -> None:
            """Test Path: happy path

        Requirement Tested:
        `review_documentation` specifies review stages in order: packet creation, scorecard completion, and proof recording.
            Standard usage: The scenario demonstrates baseline behavior.

            Verification Method: verify public function output

            Verification Detail:
            README arranges these steps in order:
            1. Scorecard creation invokes `create-agent-md`.
            2. Review completion modifies every scorecard.
            3. Proof recording invokes `lint --reviewer`.
            """

            repo_root = Path(__file__).resolve().parents[2]
            guide = (repo_root / "README.md").read_text(encoding="utf-8")
            step_markers = (
                "agentic-tdd-linter create-agent-md",
                "Review the generated files",
                "agentic-tdd-linter lint --reviewer",
            )
            step_positions = tuple(guide.index(marker) for marker in step_markers)

            self.assertEqual(tuple(sorted(step_positions)), step_positions)
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Ordered review stages belong to review documentation.
    11: # First Line States a General Behavioral Rule
      pass # The requirement states review documentation's responsibility to document the ordered review stages.
    13: # Requirement Has the Appropriate Specificity
      pass # The requirement names all three stages and explicitly requires their order.
    14: # Requirement Is Atomic
      pass # Packet creation, scorecard completion, and proof recording collectively define one review workflow.
    21: # Verification Detail States Material Outcomes
      pass # The requirement names conceptual stages while Verification Detail contains the exact commands and README wording.
    33: # Subject and Object Are Concise
      pass # `review_documentation` is the one-term subject and review stages is the two-word direct object; the colon defines those stages.
    34: # Main Verb Is Unambiguous
      pass # Specifies, demonstrates, arranges, invokes, and modifies have no common noun use in contemporary technical prose.
    37: # Sentence Expresses One Concept
      pass # The colon list defines the components of one review workflow rather than independent behaviors.

requirement_formulation_021_requirement_omits_declared:
  file_docstring: |
    """Tests in this file validate `calculator` located at `src/examples/calculator.py`.
    `calculator` is responsible for arithmetic calculations.

    For example, this file contains one test function.
    """
  test: |
    def test_adds_numbers() -> None:
        """Test Path: happy path

        Requirement Tested:
        Adding positive numbers returns a positive result.
        Standard usage: The scenario demonstrates baseline behavior.

        Verification Method: verify public function output

        Verification Detail:
        The calculated result is positive.
        """

        result = add_positive_numbers(1, 2)

        assert result > 0
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      fail # The requirement describes arithmetic behavior but omits the declared `calculator` module as its grammatical subject.

requirement_formulation_022_requirement_outside_declared:
  file_docstring: |
    """Tests in this file validate `parser` located at `src/examples/parser.py`.
    `parser` is responsible for converting text into records.
    """
  test: |
    def test_sends_account_email() -> None:
        """Test Path: happy path

        Requirement Tested:
        `parser` sends account emails.
        Standard usage: The scenario demonstrates baseline behavior.

        Verification Method: verify public function output

        Verification Detail:
        `parser` produces a successful email result.
        """

        result = parser.send_account_email()

        assert result == "sent"
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      fail # Sending account emails is outside the parser's declared text-conversion responsibility, so the test belongs in a separate module-specific test file.

requirement_formulation_023_stale_attestation_uses:
  file_docstring: |
    """Tests in this file validate `agent_review_example_runner` located at `tests/agentic_linter/test_harness/agent_review_example_runner.py`.
    `agent_review_example_runner` is responsible for orchestrating YAML-example evaluations.

    Terms:
    - `runner JSON`: Runner JSON is the readable `agent_review_example_runner` report at tests/agentic_linter/test_agent_review_example_runner.json. It contains evaluation results and the reviewer model. For example, every completed YAML-example evaluation overwrites this file.
    - `runner JSONL`: Runner JSONL is the committed proof for criteria enforced by each YAML case at tests/agentic_linter/test_agent_review_example_runner.jsonl. Each record contains its reviewer model. Current proof corresponds to every source and expected scorecard in the `YAML fixture catalog`.
    - `YAML fixture catalog`: The YAML fixture catalog is repository data evaluated by `agent_review_example_runner`. For example, each entry supplies an example and its expected scorecard as the subject of evaluation.
    - `timer start`: Timer start marks when measurement begins for the total time needed to run the agentic linter on the YAML examples. For example, the timer starts before YAML validation.
    - `timer end`: Timer end marks when runtime measurement stops after YAML scorecards have been compared. For example, elapsed review time uses timer end as its completion timestamp.
    """
  test: |
    def test_stale_attestation_uses_score_only(self) -> None:
        """Test Path: failure path

        Requirement Tested:
        `agent_review_example_runner` reports the single score-only '.agent.md' created for a stale `runner JSONL` record by its current source hash.
        Specialized usage: When one `runner JSONL` record contains 64 zero characters instead of its current YAML source hash, `agent_review_example_runner` reports the generated score-only review.

        Verification Method: verify public function output

        Verification Detail:
        1. `SCORECARD_ATTESTATION_PATH` denotes the mocked attestation with one 64-zero source hash.
        2. The `requirement_formulation_004_ambiguous_input_case` copy contains source_sha256 with 64 zero characters instead of its current `YAML fixture catalog` source hash.
        3. RuntimeError contains exactly one bullet line beginning with `- `, containing `test_<current-source-hash>__`, and ending in '.agent.md'.
        4. The artifact directory contains exactly one generated '.agent.md'.
        5. The generated packet directs the reviewer to score once and stop.
        6. The generated packet excludes the review-iteration record.

        Similar Coverage:
        - Lower Level Test: `test_render_agent_md_file.py::test_score_only_stops_after_scoring`
          Justification: Deeper coverage — The lower test directly verifies that score-only rendering excludes the review record and second-revision instruction. The current test proves stale YAML proof selects that renderer behavior.
        """

        examples_path = (
            REPO_ROOT / "tests/agentic_linter/fixtures/single_test_review"
        )
        committed_attestations = (
            REPO_ROOT
            / "tests"
            / "agentic_linter"
            / "test_agent_review_example_runner.jsonl"
        )

        with tempfile.TemporaryDirectory() as temporary_directory:
            temporary_root = Path(temporary_directory)
            anonymous_root = temporary_root / "agent_review_examples"
            artifact_root = anonymous_root / "agentic_review_artifacts"
            attestation_path = temporary_root / "stale.jsonl"
            records = [
                json.loads(line)
                for line in committed_attestations.read_text(
                    encoding="utf-8"
                ).splitlines()
            ]
            stale_record = next(
                record
                for record in records
                if record["yaml_case"] == "requirement_formulation_004_ambiguous_input_case"
            )
            current_source_hash = stale_record["source_sha256"]
            stale_record["source_sha256"] = "0" * 64
            attestation_path.write_text(
                "".join(json.dumps(record) + "\n" for record in records),
                encoding="utf-8",
            )

            with mock.patch.multiple(
                runner_harness,
                ANONYMOUS_ROOT=anonymous_root,
                ARTIFACT_ROOT=artifact_root,
                SCORECARD_ATTESTATION_PATH=attestation_path,
                REVIEW_START_PATH=anonymous_root / ".review_started_at",
            ):
                with self.assertRaises(RuntimeError) as raised:
                    run_agent_review_examples(
                        examples_path=examples_path,
                    )

            error_text = str(raised.exception)
            reported_packets = [
                line
                for line in error_text.splitlines()
                if line.startswith("- ") and line.endswith(".agent.md")
            ]
            generated_packets = tuple(artifact_root.glob("*.agent.md"))
            generated_packet = generated_packets[0].read_text(encoding="utf-8")

        self.assertEqual(1, len(reported_packets))
        self.assertEqual(1, len(generated_packets))
        self.assertIn(f"test_{current_source_hash}__", reported_packets[0])
        self.assertIn(
            "Score the current `Test Content` once against every criterion, "
            "update `## Review Scorecard`, and stop",
            generated_packet,
        )
        self.assertNotIn("## Review Iteration Record", generated_packet)
  expected_scorecard:
    17: # Scenario Has the Appropriate Specificity
      fail # Sixty-four zero characters are an incidental representation because any noncurrent source hash defines the same stale-hash scenario.
    51: # Assertions Prove the Stated Requirement
      pass # The assertions prove that stale proof generates and reports one current-hash score-only packet.

requirement_formulation_024_changed_review_contract:
  file_docstring: |
    """Tests in this file validate `CI/CD linter` located at `src/agentic_tdd_linter/cli/run_lint_pipeline.py`.
    `CI/CD linter` is responsible for validating committed review proof without creating review packets.

    Terms:
    - `CI/CD linter`: CI/CD linter runs `agentic-tdd-linter lint` in an automated pipeline against committed manifest proof. For example, it validates current proof without generating `.agent.md` files.
    - `.agent.md`: An .agent.md file contains a review scorecard that create-agent-md generates for a test. For example, CI validates committed manifest proof without recreating these files.
    """
  test: |
    def test_changed_review_contract_requires_review(self) -> None:
        """Test Path: failure path

        Requirement Tested:
        `CI/CD linter` requires a new review when a caller modifies repository review documentation after manifest approval.
        When the caller modifies README after persisting manifest proof and the repository lacks an updated `.agent.md`, `CI/CD linter` emits missing_required_agent_md.

        Verification Method: verify public function output

        Verification Detail:
        1. Harness creates a temporary repository containing one valid test and an initial README.
        2. Harness completes its review.
        3. Harness persists passing manifest proof.
        4. Harness modifies only README after manifest approval.
        5. Harness removes existing `.agent.md` files to reproduce `CI/CD linter` input.
        6. Harness invokes `agentic-tdd-linter lint --repo-root <temporary-repository> --reviewer integration:contract-reviewer`.
        7. `CI/CD linter` output contains missing_required_agent_md.

        Similar Coverage:
        - Lower Level Test: `test_build_manifest_from_agent_md_files.py::test_review_contract_changes_with_documentation`
          Justification: Deeper coverage — The lower test proves documentation changes alter the review-contract digest. The current test proves the public CI workflow requires a new review after that change.
        """

        test_source = textwrap.dedent(
            '''\
            """Tests in this file validate `documented behavior` located at `src/documented.py`.
            `documented behavior` is responsible for evaluating a reviewed boolean expression.
            """

            def test_documented_behavior() -> None:
                """Test Path: happy path

                Requirement Tested:
                `documented behavior` evaluates to true.
                Standard usage: The scenario demonstrates baseline behavior.

                Verification Method: verify public function output

                Verification Detail:
                The reviewed expression equals true.
                """

                assert True
            '''
        )

        with tempfile.TemporaryDirectory() as directory:
            repo_root = Path(directory)
            reviewer = "integration:contract-reviewer"
            _write_source(repo_root / "README.md", "Initial review workflow.\n")
            _write_source(repo_root / "src" / "documented.py", "VALUE = True\n")
            _write_source(
                repo_root / "tests" / "test_documented.py",
                test_source,
            )
            _record_approved_manifest(
                repo_root,
                reviewer=reviewer,
                review_status="pass",
                review_evidence="approved review contract",
            )
            _write_source(repo_root / "README.md", "Changed review workflow.\n")
            _remove_packet_directory(repo_root)

            lint = _run_cli(repo_root, "lint", "--reviewer", reviewer)

        self.assertIn("missing_required_agent_md", lint.stdout)
  expected_scorecard:
    17: # Scenario Has the Appropriate Specificity
      fail # README is an incidental artifact because changing any repository review documentation after manifest approval defines the same stale-review-contract scenario.
    51: # Assertions Prove the Stated Requirement
      pass # The assertion proves that stale review-contract proof causes the CI/CD linter to require new review evidence.

requirement_formulation_025_overloaded_record_default:
  file_docstring: |
    """Tests in this file validate `extracted_test_record` located at `src/agentic_tdd_linter/indexing_test_functions/extracted_test_record.py`.
    `extracted_test_record` is responsible for representing language-neutral indexed test data.

    Terms:
    - `ExtractedTestRecord`: ExtractedTestRecord is the shared data structure for one indexed test. For example, it stores a test's path, name, line, syntax node, documentation, and source without a language discriminator.
    - `record fields`: Record fields are path, name, line, syntax node, docstring, source, and a file docstring that defaults to None. For example, a caller supplies all seven fields to an extracted record.
    """
  test: |
    def test_stores_required_fields(self) -> None:
        """Test Path: happy path

        Requirement Tested:
        `extracted_test_record` represents `record fields` in `ExtractedTestRecord`, with a None file docstring when callers omit that field.
        Standard usage: The scenario demonstrates baseline behavior.

        Verification Method: verify public function output

        Verification Detail:
        Record exposes path `tests/test_sample.py`. Record exposes name `test_sample`.
        Record exposes line `1`. Record exposes node `None`.
        Record exposes docstring `Sample documentation.`.
        Record exposes source `def test_sample(): pass`.
        Record exposes file-docstring `None`.
        """

        expected_fields = (
            Path("tests/test_sample.py"),
            "test_sample",
            1,
            None,
            "Sample documentation.",
            "def test_sample(): pass",
            None,
        )
        record = ExtractedTestRecord(
            path=expected_fields[0],
            name=expected_fields[1],
            line=expected_fields[2],
            node=expected_fields[3],
            docstring=expected_fields[4],
            source=expected_fields[5],
        )
        actual_fields = (
            record.path,
            record.name,
            record.line,
            record.node,
            record.docstring,
            record.source,
            record.file_docstring,
        )

        self.assertEqual(expected_fields, actual_fields)
  expected_scorecard:
    14: # Requirement Is Atomic
      fail # The requirement combines storage of supplied record fields with defaulting an omitted file docstring, and either behavior can fail independently.
