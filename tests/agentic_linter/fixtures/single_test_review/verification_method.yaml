# Test-source examples for agent review.
# Each example contains only `file_docstring`, `test`, and `expected_scorecard`.
# Criteria omitted from `expected_scorecard` are ignored during comparison.
# Criterion comments must match their titles in `single_test_review.agent.md.j2`.
# Each outcome must be `pass` or `fail` followed by an explanation comment.

verification_method_001_public_method_receives:
  file_docstring: |
    """Tests in this file validate `linter` located at `src/examples/linter.py`.
    `linter` is responsible for validating tests.

    For example, this file contains one test function.
    """
  test: |
    def test_lint_succeeds() -> None:
        """Test Path: happy path

        Requirement Tested:
        `linter` accepts valid tests.
        Standard usage: The scenario demonstrates baseline behavior.

        Verification Method: verify public function output

        Verification Detail:
        `run_lint_pipeline` returns zero issues.
        """

        issues = run_lint_pipeline()

        assert issues == []
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Accepting valid tests belongs to the linter.
    23: # Verification Method Matches Exercised Interface
      pass # The asserted evidence comes directly from the public `run_lint_pipeline` callable.

verification_method_002_public_method_receives:
  file_docstring: |
    """Tests in this file validate `linter` located at `src/examples/linter.py`.
    `linter` is responsible for validating tests.

    For example, this file contains one test function.
    """
  test: |
    def test_lint_succeeds() -> None:
        """Test Path: happy path

        Requirement Tested:
        `linter` accepts valid tests.
        Standard usage: The scenario demonstrates baseline behavior.

        Verification Method: verify public function output

        Verification Detail:
        `_run_lint` returns zero issues.
        """

        issues = _run_lint()

        assert issues == []
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Accepting valid tests belongs to the linter.
    23: # Verification Method Matches Exercised Interface
      fail # The asserted evidence comes from the private `_run_lint` helper, not a public interface.

verification_method_003_private_method_receives:
  file_docstring: |
    """Tests in this file validate `linter` located at `src/examples/linter.py`.
    `linter` is responsible for validating tests.

    For example, this file contains one test function.
    """
  test: |
    def test_lint_succeeds() -> None:
        """Test Path: happy path

        Requirement Tested:
        `linter` accepts valid tests.
        Standard usage: The scenario demonstrates baseline behavior.

        Verification Method: verify private function output

        Verification Detail:
        `_run_lint` returns zero issues.
        """

        issues = _run_lint()

        assert issues == []
  expected_scorecard:
    16: # Requirement Fits Declared Module Responsibility
      pass # Accepting valid tests belongs to the linter.
    23: # Verification Method Matches Exercised Interface
      pass # The private verification method matches the private helper that supplies the asserted evidence.
